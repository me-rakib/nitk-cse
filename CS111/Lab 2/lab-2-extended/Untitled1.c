#include<stdio.h>
#include<math.h>
int main()
{
    float n = 0;
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=10
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=20
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=30
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=40
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=50
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=60
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=70
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=80
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=90
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    n+=10;   // n=100
    printf("Number = %.0f -> Square = %.2f & Square-root = %.2f\n",n,n*n,sqrt(n));
    return 0;
}
